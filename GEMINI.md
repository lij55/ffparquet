# GEMINI.md: ffparquet 项目分析

## 1. 项目概述

`ffparquet` 是一个使用 Rust 编写的命令行工具，用于对 Apache Parquet 文件进行高效处理。它提供了一系列功能，包括转码、元数据查看、数据预览、文件合并与拆分，以及数据查询。

从依赖项（如 `parquet`, `arrow`, `datafusion`, `object_store`）来看，该工具不仅支持本地文件，还具备与 AWS S3 等对象存储交互的能力。

## 2. 目录结构分析

项目的结构清晰，遵循了 Rust 项目的普遍约定。

-   `Cargo.toml`: 项目的清单文件，定义了项目的元数据（名称、版本）和所有依赖项。这是理解项目技术栈的关键。
-   `src/`: 存放所有源代码的目录。
    -   `main.rs`: 程序的入口点。它使用 `clap` 库来解析命令行参数，并根据用户输入的子命令来调用相应的功能模块。
    -   `cmd/`: 项目的核心逻辑目录，每个文件对应一个子命令。
        -   `mod.rs`: 声明 `cmd` 模块。
        -   `cat.rs`: 实现 `cat` 命令，用于读取并打印 Parquet 文件的内容。
        -   `meta.rs`: 实现 `meta` 命令，用于显示 Parquet 文件的元数据。
        -   `merge.rs`: 实现 `merge` 命令，用于将多个 Parquet 文件合并成一个。
        -   `split.rs`: 实现 `split` 命令，用于将单个 Parquet 文件拆分成多个。
        -   `df.rs`: 实现 `df` 命令，可能代表 "DataFrame"，利用 `datafusion` 库提供数据查询和处理能力。
        -   `utils.rs`: 存放被各个命令模块共享的通用工具函数。
-   `examples/`: 包含了一些示例代码，用于演示如何使用该工具或其库功能。
-   `target/`: Rust 的编译输出目录，包含编译后的二进制文件和中间产物。

## 3. 核心功能

该工具通过不同的子命令提供其核心功能：

-   **`cat`**: 读取 Parquet 文件的数据行并将其输出到标准输出。
-   **`meta`**: 解析并显示 Parquet 文件的详细元数据，如 Schema、行组信息、压缩方式等。
-   **`merge`**: 将多个源 Parquet 文件合并为一个目标文件。
-   **`split`**: 根据指定的策略（如行数）将一个大 Parquet 文件拆分为多个小文件。(此功能已修复并启用)
-   **`df`**: 提供一个强大的数据查询接口，允许用户使用 SQL 等方式对 Parquet 文件进行查询和分析。(此功能当前被注释，待修复)
